# Environment Configuration
# Copy this file to .env and fill in the values

# Bug report intake endpoint override (desktop dev builds only).
# Production defaults to https://voicetypr.com/api/v1/bug-reports.
# VITE_BUG_REPORT_ENDPOINT=http://localhost:3000/api/v1/bug-reports
